## git see what is about to be pused
```bash
git diff --stat --cached origin/master
```

See code diff about to be pushed
```bash
git diff origin/master 
```

List files and number of chages
```bash
git diff --numstat origin/master
```
